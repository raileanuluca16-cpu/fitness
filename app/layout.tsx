import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "EliteFit - Transform Your Body & Mind",
  description:
    "Premium fitness gym with world-class trainers, state-of-the-art equipment, and transformative programs. Join the elite.",
  generator: "v0.app",
  icons: {
    icon: "/gym-dumbbell-icon.svg",
    apple: "/gym-dumbbell-icon.svg",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
